import { App } from "obsidian";
import { QuestLogSettings } from "questlog-settings";


export async function balance(
    app: App, settings: QuestLogSettings,
    todo: any, calendar: any, goals: any) {
    // First thing I have to do is think of good formats for these
    // about 8 hours for main day,
    // but this might include work which would exist in the calendar scheduled things
    // then like 4 hours for end part of day
    // there I can focus on high priority todo's and some goal progress

}